# Crate optimization viewer — UI examples

Captured from dev demo: `http://localhost:5175/#crate-viewer-demo` (DEV only).

| File | Description |
|------|-------------|
| `crate-viewer-engineering.png` | Engineering view — isometric schematic, stack legend, metrics |
| `crate-viewer-exploded-stack.png` | Exploded stack — Layer 1–5 labels (TOPS, SPACER, BACK SPLASH, …) |
| `crate-viewer-validation-errors.png` | Blocking errors panel when stack order is violated (Apply disabled) |

Production path: **Planner → Step 3 → View Details** on a saved draft crate plan.

Mock fixture: `mock-crates.json` (generated by `frontend/scripts/capture-crate-viewer-demo.mjs`).
